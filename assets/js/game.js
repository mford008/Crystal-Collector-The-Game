//The function for when the page loads
$(document).ready(function() {

	//function for choosing a random number at the beginning of the game, between 19 and 120
	computerNumber(Math.floor(Math.random) * 120) //do I need to multiply this by an array?
	//This is the part for printing the random number to the screen
	function printComputerNumber() {
		document
	}

}